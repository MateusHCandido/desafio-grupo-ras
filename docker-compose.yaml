services:
  postgres-local:
    image: postgres:16

    container_name: tarifa_agua-local

    environment:
      POSTGRES_DB: tarifa_agua
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root

    ports:
      - "5432:5432"

    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql